# 项目状态 · Project Status

> 记录线上运行状态与待办。敏感信息（Sheet ID、收件人邮箱、部署 URL）不入库。

最后更新：2026-09-02（第二次）

## 线上状态

| 项 | 状态 |
|---|---|
| Google Sheet 数据源 | ✅ 运行中，2 期共 64 条 |
| 公开网页（Web App） | ✅ 版本 3，默认显示最新一期，历史期次侧边栏正常 |
| 周三 09:00 定时邮件 | ✅ 触发器已建（1 个，基于时间，跑 HEAD 代码） |
| 收件人 | ✅ 2 位 |
| 每周编辑流水线（定时任务） | ✅ 每周一触发 |

## 已发布期次

| WeekId | 条目 | 已核实 | 场地已核实 | 未核实 | 核实率 | 精选 |
|---|---|---|---|---|---|---|
| 2026-08-26 | 33 | 17 | 12 | 4 | 52% | 5 |
| 2026-09-02 | 31 | 23 | 6 | 2 | 74% | 9 |

第一期含 16 条社群渠道来源的活动，故核实率结构性偏低（见 功能文档 §4.2）。

第二期在 09-02 追加了 11 条（来自社群渠道积压链接，去重后净新增），
其中 2 条在任何官方渠道都查不到，按规则**保留并标注为「未核实」**，
这也是第二期核实率从 75% 落到 74% 的原因——**内容构成变了，不是质量下降**。

## 版本历史

| 版本 | 内容 |
|---|---|
| v1 | 首次部署。**线上故障**：Sheets 日期类型导致 `weekId.split is not a function` |
| v2 | 修复日期类型（新增 `cellToString_()`） |
| v3 | 侧边栏 CSS 修复（iframe 内 sticky 失效）、Pick 精选高亮、多收件人 |
| v3 之后（仅邮件，未重新部署网页） | 邮件三修：去掉扛不过邮件头编码的 emoji、`SITE_URL` 写死 `/exec`（原先动态取会拿到 `/dev`）、排版重做成与网页一致的车票视觉；新增 `previewWeeklyEmail()` |

## 待办

- [ ] **存储配额**：账号 Google 存储长期处于 99.9/100 GB。
      配额耗尽会导致 Gmail 无法发信 + Sheet 无法写入，是当前唯一的单点风险。
- [x] ~~社群渠道链接积压 5 条~~ → 09-02 全部处理完：去重命中 9 条，净新增 11 条
- [ ] **社群渠道的年份陷阱**：其中 1 条链接是**去年同月**发的，正文只写「9月5日」不写年份，
      11 条全是过期内容。已把「先滚到底确认发帖日期」写进流水线提示词，
      但校验器目前拦不住这类错误（数据本身合法，只是事实过期）
- [ ] 端到端测试缺失：渲染逻辑纯函数化后可在 Node 做快照测试
- [ ] 邮件送达无监控：触发器失败只写执行日志，读者侧表现为「邮件不来了」

## 运维备忘

**两条生效路径不同，别搞混：**
- 邮件相关代码 → 保存即生效（触发器绑定 HEAD）
- 网页相关代码 → 必须发布新版本：部署 → 管理部署 → 编辑 → 版本「新版本」→ 部署

**Apps Script Web App 首屏约需 10 秒**，白屏不等于故障。

**函数下拉框会运行「上一次」的选择。** 点完下拉项立刻点运行，选择还没提交，
跑的是上一个函数。选完**刷新页面**再运行，跑完去「脚本执行」页核对函数名——
「执行日志」不显示实际运行的是哪个函数。

**危险操作**：编辑器函数下拉框里 `sendWeeklyEmail` 紧邻 `createWeeklyTrigger` 上方，
选错点「运行」会立刻发出一封邮件。运行任何函数前先确认下拉框内容。
